global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: backend
    metrics_path: /actuator/prometheus
    static_configs:
      - targets:
          - localhost:9090  # APP_EC2_INTERNAL_IP:9090 으로 교체
